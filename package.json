{
  "name": "plasma-full-stack-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "update:all": "npm update && npm update --prefix client && npm update --prefix server",
    "audit_fix:all": "npm audit fix --force && npm audit fix --force --prefix client && npm audit fix --force --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}